{
  "name": "vtx-sdk",
  "dependencies": {},
  "devDependencies": {
    "typescript": "4.5.2"
  },
  "scripts": {
    "build:common": "yarn workspace @vtx/common build",
    "build:cli": "yarn workspace @vtx/cli build",
    "execute:cli": "yarn workspace @vtx/cli cli:execute",
    "create:cli": "yarn workspace @vtx/cli cli:create"
  },
  "workspaces": [
    "packages/*"
  ],
  "version": "1.0.0",
  "license": "MIT",
  "private": true
}
